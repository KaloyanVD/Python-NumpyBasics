import numpy as np

stats = np.array([[1, 2, 3], [1, 2, 3]])

# Get min value
print(np.min(stats))

# Get max value
print(np.max(stats))

# Sum all the elements
print(np.sum(stats))

# Get the mean value
print(np.mean(stats))
